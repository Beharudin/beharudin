import DashboardsLayout from "../(dashboards)/layout";

export default function ShemachLayout({ children }: { children: React.ReactNode }) {
  return <DashboardsLayout>{children}</DashboardsLayout>;
}